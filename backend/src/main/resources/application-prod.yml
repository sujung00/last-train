spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  data:
    redis: {}
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}

# Tasks: 즐겨찾기 페이지 구현

> 기준 문서: spec-favorite-page.md
> 작성일: 2026-07-08

---

## 작업 목록

| ID | 작업 | 관련 FR/AC | 우선순위 | 비고 |
|----|------|-----------|---------|------|
| T-001 | FavoritePage 레이아웃 + 목록 조회 | FR-001, FR-002, AC-001 | 🔴 필수 | GET /api/v1/favorites, 로딩/빈 상태 처리 |
| T-002 | 비로그인 접근 처리 | AC-002, EC-002 | 🔴 필수 | localStorage accessToken 확인 → /login 이동 |
| T-003 | "조회 →" 버튼 연동 | FR-003, AC-003, EC-003 | 🔴 필수 | GPS + 막차 조회 API 호출 → /result 이동 |
| T-004 | 편집 모달 구현 | FR-004, FR-005, AC-004, AC-005 | 🟡 권장 | 이름/이모지 수정, PATCH /api/v1/favorites/{id} ({ name, emoji }만 전송) |
| T-005 | 삭제 기능 | FR-006, AC-006 | 🟡 권장 | 편집 모달 내 삭제 버튼, DELETE API |
| T-006 | 목적지 추가 | FR-007, FR-008, AC-007, AC-008 | 🟡 권장 | PlaceSearchModal + EmojiSelectorModal 재활용 |
| T-007 | 에러 처리 | EC-004, EC-005 | 🟡 권장 | API 실패 시 에러 배너 (기존 스타일 적용) |

---

## 진행 순서 (의존성 기준)

```
T-001 (레이아웃 + 목록 조회)
  └→ T-002 (비로그인 처리)
  └→ T-003 (조회 → 버튼)
  └→ T-004 (편집 모달)
       └→ T-005 (삭제 기능)
  └→ T-006 (목적지 추가)
  └→ T-007 (에러 처리)
```

---

## 사전 확인 필요

Claude Code 시작 전 아래를 먼저 확인해줘.

```
backend/src/main/java/com/lasttrain/favorite/ 폴더를 확인해서
1. PATCH /api/v1/favorites/{id} API가 구현되어 있는지
2. 구현되어 있다면 요청 형식(RequestBody 필드명)이 무엇인지
3. DELETE /api/v1/favorites/{id} API가 구현되어 있는지
알려줘. 수정하지 말고 확인만 할 것.
```

---

## Claude Code 구현 요청 예시

```
spec-favorite-page.md를 기준으로 T-001만 구현해줘.

T-001: FavoritePage 레이아웃 + 목록 조회 (FR-001, FR-002, AC-001)
- GET /api/v1/favorites 호출해서 목록 표시
- 각 항목: 이모지 + 이름 + 주소 + "조회 →" 버튼 + "편집" 버튼
- 로딩 중 스피너 표시
- 빈 목록: "아직 즐겨찾기한 목적지가 없어요" + "+ 목적지 추가" 버튼 (EC-001)
- 헤더: "즐겨찾기" 타이틀 + "+ 추가" 버튼
- 기존 코드 스타일 (Tailwind CSS, dark navy #1a1a2e) 따를 것
- PlaceSearchModal, EmojiSelectorModal은 이번 작업에서 연결하지 말 것 (T-006에서 처리)

규칙:
1. T-001 범위만 구현할 것
2. 스펙에 없는 기능은 추가하지 말 것
3. 기존 axios 설정(api/axios.js) 사용할 것
4. 구현 후 어떤 FR/AC를 만족했는지 요약할 것
5. 불확실한 부분은 "가정"으로 명시할 것
```

---

## 구현 결과 검증 프롬프트

```
아래 구현 결과를 spec-favorite-page.md와 비교해 검증해줘.

검토 기준:
1. 만족한 요구사항
2. 누락된 요구사항
3. 스펙에 없는 기능 추가 여부
4. 엣지 케이스 처리 여부
5. 회귀 가능성

결과는 아래 표로 작성해줘.
| 항목 | 판단 | 근거 | 수정 필요 여부 |
```

---

## 완료 체크리스트

- [ ] T-001: 레이아웃 + 목록 조회
- [ ] T-002: 비로그인 처리
- [ ] T-003: 조회 → 버튼
- [ ] T-004: 편집 모달
- [ ] T-005: 삭제 기능
- [ ] T-006: 목적지 추가
- [ ] T-007: 에러 처리